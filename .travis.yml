language: python
python:
  - "3.9"

env:
  - DJANGO=3.1.7 DB=postgresql
  - SECRET_KEY="TEST"
services:
  - postgresql
install:
  - pip install -r requirements.txt
# before_script: 
#   - psql -c 'create database travis_ci_test;' -U postgres
script: 
  - python manage.py test
